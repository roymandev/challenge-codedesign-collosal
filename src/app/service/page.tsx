import SectionTitle from './SectionTitle';
import SectionUiDesign from './SectionUiDesign';

const page = () => {
  return (
    <main className="px-4">
      <SectionTitle />
      <SectionUiDesign />
    </main>
  );
};

export default page;
